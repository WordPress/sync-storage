<?php
/**
 * WordPress core symbols missing from php-stubs/wordpress-stubs.
 *
 * The stub package is generated from core's function and class declarations,
 * so constants core defines at runtime -- wp-includes/default-constants.php,
 * wp-includes/wp-db.php -- are absent and have to be declared here.
 *
 * Scanned, never analysed. Add a constant only once something in lib/ uses it.
 *
 * @package Sync_Storage
 */

define( 'ARRAY_A', 'ARRAY_A' );
define( 'DAY_IN_SECONDS', 86400 );

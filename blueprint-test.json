{
	"$schema": "https://playground.wordpress.net/blueprint-schema.json",
	"preferredVersions": {
		"wp": "latest"
	},
	"landingPage": "/wp-admin/post-new.php",
	"login": true,
	"steps": [
		{
			"step": "installPlugin",
			"pluginData": {
				"resource": "wordpress.org/plugins",
				"slug": "presence-api"
			},
			"options": {
				"activate": true
			}
		},
		{
			"step": "installPlugin",
			"pluginData": {
				"resource": "url",
				"url": "https://github.com/josephfusco/gutenberg/archive/refs/heads/add/pluggable-sync-storage.zip"
			},
			"options": {
				"activate": true
			}
		},
		{
			"step": "installPlugin",
			"pluginData": {
				"resource": "url",
				"url": "https://github.com/josephfusco/realtime-collaboration/releases/latest/download/realtime-collaboration.zip"
			},
			"options": {
				"activate": true
			}
		},
		{
			"step": "setSiteOptions",
			"options": {
				"wp_collaboration_enabled": "1"
			}
		},
		{
			"step": "runPHP",
			"code": "<?php require_once 'wordpress/wp-load.php'; add_post_type_support( 'post', 'presence' ); $users = array( array('login' => 'editor1', 'email' => 'editor1@example.com', 'display_name' => 'Alice Editor'), array('login' => 'editor2', 'email' => 'editor2@example.com', 'display_name' => 'Bob Editor'), array('login' => 'editor3', 'email' => 'editor3@example.com', 'display_name' => 'Carol Editor') ); foreach ( $users as $user_data ) { $user_id = wp_insert_user( array( 'user_login' => $user_data['login'], 'user_email' => $user_data['email'], 'display_name' => $user_data['display_name'], 'user_pass' => 'password', 'role' => 'editor' ) ); }"
		},
		{
			"step": "writeFile",
			"path": "/wordpress/wp-content/mu-plugins/rtc-demo-helper.php",
			"data": "<?php\n/**\n * RTC Demo Helper\n * Adds a notice explaining how to test collaborative editing in Playground.\n */\nadd_action( 'admin_notices', function() {\n    $screen = get_current_screen();\n    if ( $screen && $screen->base === 'post' ) {\n        echo '<div class=\"notice notice-info\"><p><strong>RTC Demo:</strong> Open this post in 2+ tabs to see collaborative editing activate. Or login as different users (editor1/editor2/editor3, password: password) in separate browsers/incognito windows.</p></div>';\n    }\n});\n"
		}
	]
}
